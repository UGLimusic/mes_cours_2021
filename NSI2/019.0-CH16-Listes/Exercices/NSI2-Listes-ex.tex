\documentclass[a4paper,12pt,french]{article}
\usepackage[margin=2cm]{geometry}
\usepackage[thinfonts]{uglix2}
\nouveaustyle

\begin{document}
\titre{Listes - Exercices}{NSI2}{2021}

\begin{exercice}
Ouvrir le fichier \texttt{LinkedList\_stump.py} (\textit{stump} veut dire moignon en anglais) et regarder en détail l'implémentation de la structure de liste chaînée vue en cours.\\
Pour l'utiliser inclure \pythoninline{from LinkedList_stump import LinkedList} dans votre code.
    \'Ecrire une fonction \pythoninline{liste_N} qui
    \begin{enumerate}[--]
        \item en entrée prend un \pythoninline{int} n;
        \item renvoie la liste chaînée composée des éléments 1, 2, ..., n.
    \end{enumerate}
\end{exercice}
\begin{exercice}[ : Terminer l'implémentation objet]
Le fichier \texttt{LinkedList\_stump.py}  est incomplet. Tu vas devoir le compléter. Une fois ceci fait tu pourras lui donner le nom \texttt{LinkedList.py}.
\begin{enumerate}[\bfseries 1.]
    \item À quoi sert la méthode \pythoninline{__getitem__} ?
    \item En s'inspirant de la méthode précédente, écrire la méthode \pythoninline{find}.
    \item Coder la méthode \pythoninline{extend} qui
          \begin{enumerate}[--]
              \item en entrée prend une deuxième liste chaînée;
              \item ajoute tous les éléments de cette liste à la première, dans l'ordre
          \end{enumerate}
            Par exemple, si \pythoninline{L} vaut \texttt{<1, 2, 4>} et si \pythoninline{L2} vaut \texttt{<6, 5>} alors \pythoninline{L.extend(L2)} vaut \texttt{<1, 2, 4, 6, 5>}.
     \item Coder la méthode \pythoninline{__eq__} qui
     \begin{enumerate}[--]
         \item en entrée prend une seconde liste chaînée;
         \item renvoie \pythoninline{True} si les deux listes ont les mêmes éléments aux mêmes places et \pythoninline{False} sinon.
     \end{enumerate}
\end{enumerate}
\end{exercice}

\begin{exercice}[ : Algorithmes récursifs]
\'Ecrire un script \texttt{recursive\_functions.py} qui importe \texttt{LinkedList.py} et
\begin{enumerate}[\bfseries 1.]
    \item \'Ecrire la fonction \pythoninline{recursive_length} qui
    \begin{enumerate}[--]
        \item en entrée prend une liste chainée;
        \item en sortie renvoie un \pythoninline{int} qui est sa longueur en procédant de manière récursive.
    \end{enumerate}
    \item \'Ecrire la fonction \pythoninline{recursive_find} qui
    \begin{enumerate}[--]
    \item en entrée prend une liste chainée et un élément;
    \item en sortie renvoie un \pythoninline{int} qui est la position de l'élément s'il figure dans la liste, et -1 sinon.\\
            Cette fonction procède récursivement.
\end{enumerate}
\textbf{Conseil :} Pour chacune de ces fonctions, écrire une sous-fonction récursive qui procède sur des données de type \pythoninline{Cell} et qui sera appelée par la fonction principale.
\end{enumerate}
\end{exercice}
\end{document}